<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <%= link_to 'Home', root_path, class:'navbar-brand' %>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <% if user_signed_in? %>
            <%= link_to 'Update Profile', edit_user_registration_path, class:'nav-link' %>
            <%= link_to 'Messages', mensajes_index_path, class:'nav-link' %>
            <%= link_to 'Bookings', bookings_path, class:'nav-link'%>
            <%= link_to 'End Session', destroy_user_session_path, method: :delete, class:'nav-link' %>
        <% else %>
            <%= link_to 'Sign up', new_user_registration_path, class:'nav-link' %>
            <%= link_to 'Log In', new_user_session_path, class:'nav-link' %>
        <% end %>
      </ul>
      <form class="d-flex">
        <% if current_user && current_user.admin %>
            </h4><%= link_to 'Admin Dashboard ', admin_root_path %></h4>
            <h4> Welcome Administrator <%= current_user.name.capitalize %></h4>
        <% elsif current_user && current_user.moderator %>
            <h4> Welcome Moderator <%= current_user.name.capitalize %></h4>
        <% elsif current_user %>
            <h4> Welcome <%= current_user.name.capitalize %>!</h4>
        <% else %>
            <h4> Welcome Guest!</h4>
        <% end %>
      </form>
    </div>
  </div>
</nav>